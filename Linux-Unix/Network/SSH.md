The SSH protocol allows for remote connection to another Linux system.
- Run `ip addr` (or `ifconfig`) to determine the address of the machine (ignore the loopback device and ethernet).
- Run `ssh -l <USERNAME> <IP ADDRESS>`, then follow the instructions on the console to connect to the system remotely.